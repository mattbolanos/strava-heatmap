import SwiftUI

enum Theme {
    static let stravaOrange = Color(red: 0.988, green: 0.322, blue: 0.0)
    static let cardBackground = Color(UIColor.secondarySystemGroupedBackground)
    static let subtleOrange = stravaOrange.opacity(0.12)
}
